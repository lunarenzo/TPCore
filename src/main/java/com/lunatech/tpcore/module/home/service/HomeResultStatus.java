package com.lunatech.tpcore.module.home.service;

public enum HomeResultStatus {
    SUCCESS,
    HOME_NOT_FOUND,
    OVERWRITE_REQUIRED,
    LIMIT_REACHED,
    WORLD_RESTRICTED,
    UNSAFE_LOCATION,
    ALREADY_SHARED,
    NOT_SHARED,
    ERROR
}
